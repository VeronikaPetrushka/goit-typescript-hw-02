const ErrorMessage: React.FC = () => {
    return (
        <p style={{fontSize: 20, color: "black"}}>Whooops...Something went wrong, please reload the page!</p>
    );
};

export default ErrorMessage;